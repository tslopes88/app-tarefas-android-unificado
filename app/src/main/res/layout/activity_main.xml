<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    android:padding="16dp">

    <!-- Cabeçalho -->
    <LinearLayout
        android:id="@+id/layoutCabecalho"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txtTituloApp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Minhas Tarefas"
                android:textColor="@color/colorText"
                android:textSize="22sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/txtContadorTarefas"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pendentes: 0 | Concluídas: 0"
                android:textColor="@color/colorSecondary"
                android:textSize="12sp"
                android:textStyle="bold" />
        </LinearLayout>

        <TextClock
            android:id="@+id/relogioDigitalHora"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@color/colorCard"
            android:format12Hour="HH:mm"
            android:format24Hour="HH:mm"
            android:paddingStart="12dp"
            android:paddingTop="6dp"
            android:paddingEnd="12dp"
            android:paddingBottom="6dp"
            android:textColor="@color/colorSecondary"
            android:textSize="18sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Formulário Dinâmico -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardFormulario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        app:cardBackgroundColor="@color/colorCard"
        app:cardCornerRadius="10dp"
        app:cardElevation="3dp"
        app:layout_constraintTop_toBottomOf="@id/layoutCabecalho">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <TextView
                android:id="@+id/txtStatusFormulario"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Nova Tarefa"
                android:textColor="@color/colorSecondary"
                android:textSize="12sp"
                android:textStyle="bold" />

            <EditText
                android:id="@+id/editNome"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:hint="Nome da tarefa (mín. 3 caracteres)"
                android:maxLength="50"
                android:textColor="@color/colorText"
                android:textColorHint="@color/colorTextMuted"
                android:textSize="14sp" />

            <EditText
                android:id="@+id/editDescricao"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:hint="Descrição (opcional)"
                android:maxLength="200"
                android:textColor="@color/colorText"
                android:textColorHint="@color/colorTextMuted"
                android:textSize="14sp" />

            <!-- Seletores -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:orientation="horizontal">

                <Button
                    android:id="@+id/btnSelecionarData"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="2dp"
                    android:layout_weight="1"
                    android:backgroundTint="@color/colorSurfaceSecondary"
                    android:text="📅 Data"
                    android:textColor="@color/colorText"
                    android:textSize="11sp" />

                <Button
                    android:id="@+id/btnSelecionarHora"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="2dp"
                    android:layout_marginEnd="2dp"
                    android:layout_weight="1"
                    android:backgroundTint="@color/colorSurfaceSecondary"
                    android:text="⏰ Hora"
                    android:textColor="@color/colorText"
                    android:textSize="11sp" />

                <Button
                    android:id="@+id/btnPrioridade"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="2dp"
                    android:layout_weight="1"
                    android:backgroundTint="@color/colorPriorityMedium"
                    android:text="🟡 Média"
                    android:textColor="@color/colorBackground"
                    android:textSize="11sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:orientation="horizontal">

                <Button
                    android:id="@+id/btnCancelarEdicao"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="4dp"
                    android:layout_weight="1"
                    android:backgroundTint="@color/colorSurfaceSecondary"
                    android:text="Cancelar"
                    android:textColor="@color/colorText"
                    android:visibility="gone" />

                <Button
                    android:id="@+id/btnSalvar"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="2"
                    android:backgroundTint="@color/colorPrimary"
                    android:text="Adicionar Tarefa"
                    android:textColor="@color/colorText" />
            </LinearLayout>

        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Barra de Filtros -->
    <LinearLayout
        android:id="@+id/layoutFiltros"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/cardFormulario">

        <Button
            android:id="@+id/btnFiltroTodas"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_marginEnd="2dp"
            android:layout_weight="1"
            android:backgroundTint="@color/colorPrimary"
            android:text="Todas"
            android:textColor="@color/colorText"
            android:textSize="11sp" />

        <Button
            android:id="@+id/btnFiltroPendentes"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_marginStart="2dp"
            android:layout_marginEnd="2dp"
            android:layout_weight="1"
            android:backgroundTint="@color/colorSurfaceSecondary"
            android:text="Pendentes"
            android:textColor="@color/colorText"
            android:textSize="11sp" />

        <Button
            android:id="@+id/btnFiltroConcluidas"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_marginStart="2dp"
            android:layout_weight="1"
            android:backgroundTint="@color/colorSurfaceSecondary"
            android:text="Concluídas"
            android:textColor="@color/colorText"
            android:textSize="11sp" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewTarefas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/layoutFiltros"
        tools:listitem="@layout/tarefa_individual" />

    <TextView
        android:id="@+id/txtListaVazia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nenhuma tarefa por aqui! 🚀"
        android:textColor="@color/colorTextEmptyState"
        android:textSize="15sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/recyclerViewTarefas"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/recyclerViewTarefas" />

</androidx.constraintlayout.widget.ConstraintLayout>